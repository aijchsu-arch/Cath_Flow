# CLAUDE.md

本檔案提供 Claude Code 在此 repo 工作時的專案背景與開發準則。

## 專案名稱

床邊心臟超音波簡易報告系統(Bedside Echo Report)

## 專案背景

- **使用者**:推超音波機至床邊檢查的心臟科醫師。
- **使用情境**:醫師用手機(經 VPN 連入院內網路)拍攝超音波畫面照片或影片,
  搭配語音口述與常用片語,快速產生簡易報告,以病歷號連結,供事後上傳與簽收。

## 技術架構

- **前端**:React + Vite + TypeScript,做成 PWA(可加入手機主畫面)。
  程式碼位於 `bedside-echo/` 目錄。
- **後端**:之後會用 Node.js,資料庫由院方資訊室維護。
- **病人資料**:透過院內 HIS 以 FHIR (R4) 介接,用病歷號帶出病人基本資料。
- **登入**:院內醫師 HIS 帳號密碼(後續階段實作)。
- **部署**:院內伺服器,院內 HTTPS 憑證,僅限院內網路存取。
- **影像保存**:依規定保存 7 年。

## 開發原則

- 一律使用**虛構測試資料**,不使用真實病人資料。
- 介面使用**繁體中文**。
- **手機優先設計**:醫師單手操作、白袍口袋掏出手機就能用
  (大按鈕、大字體、數字鍵盤優先)。
- 每完成一個功能就 `git commit`。
- 手機相機等功能需要 HTTPS,開發環境已設定自簽憑證(見 `bedside-echo/README.md`)。

## 開發階段

1. **拍照上傳介面**(✅ 已完成:PWA 基本設定、首頁病歷號查詢、拍照/相簿選取、
   註記、模擬上傳)
2. **錄影上傳**(✅ 已完成:MediaRecorder 無聲錄影、單段 60 秒上限、
   相簿選取影片備援、多段管理與全螢幕回放、照片+影片合併為單一檢查紀錄、
   總大小統計與 200MB 警告、上傳服務介面 `ExamUploadService`
   以 `TODO(上傳)` 標注之後的分段上傳與續傳)
3. 常用片語面板
4. 語音轉文字
5. 後端與 FHIR 介接(目前以寫死的假資料模擬 FHIR 回傳,
   程式中以 `TODO(FHIR)` 標注待接真實 API 之處)
6. 簽收流程

**目前完成第一、二階段;後續階段尚未開始。**

## Repo 結構備註

- repo 根目錄的 `index.html` 與 `CathFlow排程工作台.html` 是既有的
  GitHub Pages 內容(CathFlow 排程工作台),與本 PWA 無關,請勿更動。
- 本 PWA 的所有程式碼與設定都在 `bedside-echo/` 目錄內。

## 常用指令(於 `bedside-echo/` 目錄執行)

```bash
npm install        # 安裝相依套件
npm run dev        # 啟動 HTTPS 開發伺服器(含 --host,供手機實測)
npm run build      # 型別檢查 + production build
npm run preview    # 預覽 production build
```
